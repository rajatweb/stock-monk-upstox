export interface IUser {
    email: string
    exchanges: string[]
    products: string[]
    broker: string
    user_id: string
    user_name: string
    order_types: string[]
    user_type: string
    poa: boolean
    is_active: boolean
    access_token: string
    extended_token: any
}
